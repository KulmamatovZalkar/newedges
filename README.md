# Telegram Bot "Новые грани" - Staff

Telegram бот для школы "Новые грани" с использованием aiogram, Django и Docker.

## 🚀 Быстрый старт

### 1. Клонируйте репозиторий

```bash
git clone <repo-url>
cd "новые грани бот"
```

### 2. Создайте файл .env

```bash
cp .env.example .env
```

Отредактируйте `.env`:
```env
# Получите токен у @BotFather
BOT_TOKEN=ваш_токен

# Пароли для базы данных
DB_PASSWORD=ваш_надежный_пароль

# Django секретный ключ (сгенерируйте новый!)
SECRET_KEY=ваш_секретный_ключ

# Учетные данные админа
DJANGO_SUPERUSER_USERNAME=admin
DJANGO_SUPERUSER_EMAIL=admin@example.com
DJANGO_SUPERUSER_PASSWORD=ваш_пароль_админа
```

### 3. Запустите

```bash
docker-compose up -d --build
```

При первом запуске автоматически:
- ✅ Применятся миграции
- ✅ Создастся суперпользователь
- ✅ Загрузятся начальные вопросы

### 4. Доступ

- **Админка:** http://localhost:8000/admin/
- **Бот:** найдите вашего бота в Telegram

---

## 📁 Структура проекта

```
├── docker-compose.yml      # Docker конфигурация
├── .env.example            # Шаблон переменных окружения
├── django_app/             # Django + Admin
│   ├── Dockerfile
│   ├── entrypoint.sh       # Автоматический setup
│   ├── config/             # Настройки Django
│   └── bot_admin/          # Приложение бота
│       ├── models.py       # Question, UserProfile, StaffApplication
│       └── admin.py        # Админка
└── telegram_bot/           # Telegram бот (aiogram 3.x)
    ├── Dockerfile
    ├── bot.py              # Точка входа
    ├── database.py         # Работа с БД
    └── handlers/           # Обработчики
```

---

## 🔧 Полезные команды

```bash
# Логи
docker-compose logs -f bot      # Логи бота
docker-compose logs -f django   # Логи Django

# Управление
docker-compose restart          # Перезапуск
docker-compose down             # Остановка
docker-compose up -d --build    # Пересборка

# База данных
docker-compose exec django python manage.py createsuperuser  # Новый админ
docker-compose exec django python manage.py migrate          # Миграции
```

---

## 🎯 Для продакшена

1. Установите `DEBUG=0` в `.env`
2. Добавьте ваш домен в `ALLOWED_HOSTS`
3. Смените все пароли на надёжные
4. Используйте nginx как reverse proxy
# newedges
